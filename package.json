{
  "name": "makeup-by-roopal-goel",
  "version": "1.0.0",
  "private": true,
  "description": "Luxury bridal makeup portfolio site for Makeup by Roopal Goel",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "node server/server.js",
    "deploy": "npm run install:all && npm run build && pm2 start ecosystem.config.js",
    "deploy:restart": "npm run install:all && npm run build && pm2 restart makeup-roopal"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
